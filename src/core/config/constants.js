// These values are intentionally null.
// All contact info (WhatsApp, Instagram, etc.) must be configured
// by the platform owner via Admin > Configuración.
// They are served dynamically from tenant.settings via SiteConfigContext.
export const TEVRA_SUPPORT_WHATSAPP = null;
export const TEVRA_INSTAGRAM_URL = null;
